server:
  port: 18002
spring:
  application:
    name: geek-task-service
  datasource:
    url: jdbc:mysql://localhost:3306/geek?useUnicode=true&characterEncoding=utf8
    username: root
    password: 1234
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jooq:
    sql-dialect: org.hibernate.dialect.MySQL5Dialect

